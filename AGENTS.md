# 乐山幺鸡麻将算分器 - 需求拆解文档

## 产品概述

- **产品类型**: 工具类应用（麻将算分器）
- **场景类型**: <scene_type>prototype-app</scene_type>
- **目标用户**: 乐山幺鸡麻将玩家、牌友
- **核心价值**: 根据牌型、幺鸡数量、加番项等参数，实时准确计算番数和赔付金额，避免人工算分出错
- **界面语言**: 中文
- **主题偏好**: user_specified（麻将主题，绿色/红色配色，中国风元素）
- **导航模式**: 无导航（单页工具应用）
- **导航布局**: 无

---

## 页面结构总览

> **说明**：单页工具应用，所有交互区域垂直排列，移动端友好

**页面文件**: `MahjongCalculatorPage.tsx`

| 区域 | 说明 |
|-----|------|
| 顶部标题区 | 标题"乐山幺鸡麻将算分器" + 麻将主题装饰元素 |
| 基础设置区 | 底分输入、起胡番数选择、封顶番数选择 |
| 牌型选择区 | 6种基础牌型单选 |
| 加番选项区 | 多个加番项复选 + 杠数/根数数字选择 |
| 幺鸡设置区 | 幺鸡数量选择 + 是否用作万能牌开关 + 鸡番说明 |
| 结果展示区 | 番数明细、赔付金额、特殊牌型提示 |
| 规则说明区 | 可折叠的规则详情 |

---

## 页面布局建议

- **布局模式**: 单栏纵向布局（移动端优先），桌面端内容区居中限宽（max-width 约 640px）
- **视觉重心**: 结果展示区（突出总番数和金额，大号字体醒目）
- **结果承载区**: 结果卡片面板，位于页面中下部，所有参数选择后实时更新；初始态显示默认参数（平胡、底分1元、0番起胡、5番封顶）的计算结果
- **交互节奏**: 自上而下依次为「设置 → 牌型 → 加番 → 幺鸡 → 结果」，符合用户思考顺序

---

## 数据来源声明

| 数据/操作 | 来源类型 | 实现要求 | mock 兜底 |
|---|---|---|---|
| 算分参数与结果 | demo-mock | 所有计算逻辑在前端 state 中实时计算，初始默认值（平胡、底分1元、0番起胡、5番封顶） | ✅ 本身就是前端规则计算，无需后端 |
| 用户偏好设置（底分/起胡/封顶） | local-persist | localStorage key=`__app_leshan_mahjong_settings`，保存用户上次设置的底分、起胡番数、封顶番数 | 无（首次访问使用默认值） |

> 类型选择说明：本应用为纯前端规则计算工具，所有算分逻辑由 JavaScript 规则引擎完成，属于结构化数据 + 数值计算，不需要后端或插件支持。

---

## 功能列表

- **页面**: 乐山幺鸡麻将算分器
  - **页面目标**: 用户选择牌型和参数后，实时查看番数和赔付金额
  - **功能点**:
    - **基础设置**: 底分数字输入（支持自定义数字，默认1）、起胡番数单选（0番/1番）、封顶番数单选（5番/6番/8番/9番）
    - **牌型选择**: 6种基础牌型单选（平胡/对对胡/清一色/七对/带幺九/将对），选中即切换基础番数
    - **加番选项**: 自摸、杠上花、杠上炮、抢杠胡、金钩钓 多选开关；杠数量（0-4）、根数量（0-4）步进选择
    - **幺鸡设置**: 幺鸡数量（0-4）步进选择；是否用作万能牌开关；实时显示鸡番规则说明
    - **实时算分**: 任一参数变更立即重新计算，展示基础番数、加番合计、鸡番/无鸡番调整说明、总番数（含封顶标识）、单家赔付、自摸总收益、点炮赔付、特殊牌型提示（精品/豪金）
    - **规则说明折叠**: 点击展开/收起完整算分规则详情

---

## 算分逻辑说明（供开发参考）

### 计算流程
1. **基础番数** = 选中牌型对应的番数（平胡1、对对胡2、清一色4、七对4、带幺九2、将对2）
2. **加番合计** = 自摸(1) + 杠上花(1) + 杠上炮(1) + 抢杠胡(1) + 金钩钓(1) + 杠数量×1 + 根数量×1
3. **初步总番** = 基础番数 + 加番合计
4. **幺鸡调整**:
   - 幺鸡数量 = 0 → 无鸡加番：初步总番 + 1
   - 幺鸡数量 > 0 且 未用作万能牌 → 鸡番加倍：初步总番 × 2
   - 幺鸡数量 > 0 且 用作万能牌 → 不调整
5. **封顶处理**: 若调整后番数 > 封顶番数 → 总番数 = 封顶番数，标注"（封顶）"
6. **起胡校验**: 若总番数 < 起胡番数 → 提示"不够胡"或显示0元
7. **赔付计算**:
   - 单家赔付 = 底分 × 2^总番数
   - 自摸总收益 = 单家赔付 × 3
   - 点炮赔付 = 单家赔付 × 1
8. **特殊牌型**:
   - 幺鸡数量 = 3 → 显示"精品（3只鸡）"
   - 幺鸡数量 = 4 → 显示"豪金（4只鸡）"

-------

<scene_type>prototype-app</scene_type>

# UI 设计指南

## 1. 设计推导依据

- **参考意图**: Free Direction —— 无参考材料，从麻将产品语义与中国风主题出发自主设计
- **核心情绪 / 应用类型**: 工具型娱乐应用，牌局间隙快速算分，需要清晰、喜庆、有麻将桌面氛围
- **独特记忆点**: 麻将牌面式按钮 + 幺鸡图标贯穿关键状态，结果区用"牌桌红底 + 烫金数字"呈现番数与金额

## 2. Art Direction

- **方向名**: 麻将桌中国风
- **Design Style**: Neo-Chinese 新中式 + Soft Blocks 柔色块 —— 以麻将桌面绿为基底、中国红为强调，融入牌面、回纹、幺鸡等麻将符号，既喜庆又不干扰算分
- **DNA 参数**: 圆角中等（`rounded-lg`）/ 阴影轻到中（`shadow-md`）/ 间距标准（`gap-4 p-6`）/ 字体方向：正文无衬线清晰，标题带书法气质 / 装饰手法：麻将牌面按钮、幺鸡 icon、回纹边角、竹席纹理背景
- **应用类型**: Tool —— 单页纵向表单 + 结果卡，移动端优先

## 3. Color System

**色彩关系**: 竹青绿主色 + 麻将桌深绿背景 + 中国红强调 + 米白牌面 + 烫金高亮
**配色设计理由**: 绿色来自麻将桌面与竹牌底色，承担品牌与主交互；红色来自中国风喜庆与"胡牌"情绪，用于结果高亮与关键数字；米白模拟牌面，承载表单内容；烫金用于总番数等核心数字，强化仪式感
**主色推导**: 从麻将竹牌的青绿与牌桌绒布深绿提取 primary，辅以中国红 accent，整体色温润不刺眼，符合娱乐工具的轻松氛围
**使用比例**: 60% 中性（米白/深绿底）/ 30% 竹青绿辅助 / 10% 中国红 + 烫金强调；primary 用于主按钮与牌型选中态，红色只用于结果区与胡牌提示

| 角色 | CSS 变量 | Tailwind Class | HSL 值 | 设计说明 |
|---|---|---|---|---|
| bg | `--background` | `bg-background` | hsl(140 25% 18%) | 深麻将桌绿，页面背景带细微竹席纹理 |
| card | `--card` | `bg-card` | hsl(45 35% 96%) | 米白牌面色，卡片与表单承载面 |
| text | `--foreground` | `text-foreground` | hsl(30 10% 12%) | 深墨色，标题与正文高对比 |
| textMuted | `--muted-foreground` | `text-muted-foreground` | hsl(30 6% 40%) | 灰棕色，说明文字与辅助信息 |
| primary | `--primary` | `bg-primary` / `text-primary` | hsl(145 45% 32%) | 竹青绿，主交互、牌型选中、品牌识别 |
| primaryForeground | `--primary-foreground` | `text-primary-foreground` | hsl(45 40% 98%) | 米白色，primary 上的文字 |
| accent | `--accent` | `bg-accent` | hsl(355 70% 42%) | 中国红，结果高亮、胡牌状态、幺鸡强调 |
| accentForeground | `--accent-foreground` | `text-accent-foreground` | hsl(45 50% 98%) | 米白色，accent 上的文字 |
| border | `--border` | `border-border` | hsl(35 15% 78%) | 浅棕黄，牌面边界与分隔线 |

**语义色提示**: 成功（胡牌/自摸）复用 primary 竹青绿三态：bg `hsl(145 50% 92%)` / border `hsl(145 45% 65%)` / text `hsl(145 50% 25%)`；警告（封顶/起胡不足）用暖橙三态：bg `hsl(35 90% 92%)` / border `hsl(35 85% 62%)` / text `hsl(25 80% 35%)`，饱和度与 primary 对齐 ±10%；错误（无效组合）用红棕三态：bg `hsl(355 60% 92%)` / border `hsl(355 65% 70%)` / text `hsl(355 55% 35%)`，避免纯红刺眼；烫金数字用 `hsl(42 80% 45%)` 仅用于总番数与金额大号展示

## 4. 字体与节奏

- **font-display**: Ma Shan Zheng —— 粗毛笔质感，用于标题"乐山幺鸡麻将算分器"与总番数大号数字，传递中国风与牌局仪式感
- **font-body**: Noto Sans SC —— 清晰易读，用于所有表单选项、说明文字与结果明细，保证手机端快速扫读
- **字号**: H1 text-4xl ~ text-5xl（Ma Shan Zheng）；H2 text-xl ~ text-2xl；body text-base；muted text-sm；总番数展示 text-5xl ~ text-6xl
- **圆角**: 中等（`rounded-lg`）—— 模拟麻将牌的圆润边角，不尖锐也不卡通

## 5. 全局布局契约

- **Reference Layout Use**: 按需求结构推导，6 个交互区自上而下纵向排列
- **Page / Section Order**: 顶部标题 → 基础设置 → 牌型选择 → 加番选项 → 幺鸡设置 → 结果展示 → 规则说明（折叠）
- **Standard Content Zone**: `max-w-xl mx-auto`，移动端占满宽度，桌面端居中收窄，符合单手操作与聚焦算分
- **Shell / Frame Alignment**: 单页无 chrome，内容区独立居中，背景全宽铺深绿纹理
- **Padding & Rhythm**: `px-4 md:px-6 py-8 md:py-10`，section 间距 `gap-5`，卡片内边距 `p-5`
- **Full-bleed Zones**: 页面背景与标题装饰条全宽，所有卡片与表单内容受 `max-w-xl` 约束
- **Local Narrowing**: 结果展示区金额数字可单独放大居中，不受表单网格限制
- **Overflow Strategy**: 牌型与加番选项使用网格换行，无需横向滚动
- **Flexibility Boundary**: 允许移动端卡片内边距减至 `p-4`、网格列数从 3 变 2；不允许改变主色、圆角、阴影语言与纵向区块顺序

## 6. 视觉与动效

- **装饰**: 麻将牌面按钮（浅米白底 + 深绿字 + 细边框 + 选中时绿底反白）、幺鸡 SVG 图标、回纹边角装饰、结果区红底烫金
- **阴影/边界**: 轻到中 —— 卡片 `shadow-md` 模拟牌面凸起，按钮 `shadow-sm`，选中态加内阴影
- **动效**: 克制 —— 选项切换 150ms 背景色过渡，结果数字变化 200ms 数字跳动，折叠面板 200ms 高度过渡，无多余入场动画

## 7. 组件原则

- 牌型选择用麻将牌面式按钮组（单选）：默认米白底深绿字，选中绿底白字加内阴影
- 加番选项用圆角 checkbox / toggle：选中态用 primary 绿，带勾号反馈
- 数字输入（底分、杠数、根数、幺鸡数量）用左右加减步进器，牌面风格
- 结果卡片用红底 + 烫金大号数字，明细行用深灰文字左对齐
- 所有交互元素必须有 `:focus-visible` 金色描边状态
- 折叠面板（规则说明）默认收起，标题带箭头指示

## 8. Image Direction

- **Image Role**: 装饰性背景纹理 + 幺鸡图标（SVG 为主，非位图）
- **Image Art Direction**: 背景为深绿色竹席编织纹理，低对比度、不抢内容；幺鸡图标为简约线条风格，一条麻将牌上的公鸡图案，中国传统剪纸气质，用金色与红色点缀；所有图片信息权重低，服务氛围不干扰算分
- **Image Prompt Keywords**: 深绿色竹席纹理、麻将桌绒布质感、幺鸡一条剪纸图标、中国传统回纹边角、米白麻将牌面、烫金数字效果、中式喜庆简约
- **Image Avoidance**: 避免卡通麻将人物、花哨的财神/元宝素材、高饱和渐变背景、通用棋牌游戏宣传图风格、复杂花纹抢文字可读性

## 9. Anti-patterns

- **Split personality**: 结果区用红金、表单区又跳回默认 SaaS 蓝；全站统一竹青绿 + 中国红 + 米白牌面三色系统
- **Phantom tokens**: 编造不存在的麻将主题 CSS 变量；只使用定义的 9 个基础 token + 语义色
- **Default SaaS drift**: 回到默认蓝按钮、圆角卡片堆叠；用麻将牌面按钮、红金结果卡、竹席背景建立产品识别
- **Invisible interaction**: 牌型按钮只改颜色没有 focus 态；所有可交互元素必须有金色描边 `focus-visible`
- **Mono-hue tyranny**: 绿色铺满按钮、tab、图标、边框、链接；primary 只用于主交互与选中态，结果强调交给红色与金色
- **Status color drift**: 错误提示用刺眼纯红，成功用荧光绿；语义色饱和度与 primary 对齐，走温润中式色调